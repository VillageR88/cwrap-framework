{
  "context": "{{let routes = cwrapReference.globals.const.documentation['getting started'].concat(cwrapReference.globals.const.documentation['advanced concepts']); routes = routes.map((x)=>`documentation/${x}/`);cwrapContext.set('documentRoutes',routes);return '';}}",
  "routes": "{{{ return { \"routes\": cwrapContext.get('documentRoutes') }; }}}",
  "element": "cwrap-template",
  "text": "cwrapTemplate[documentation]",
  "passoverRef": "documentation",
  "passover": [
    {
      "element": "cwrap-template",
      "text": "cwrapTemplate[{{return cwrapRoute}}]"
    }
  ]
}